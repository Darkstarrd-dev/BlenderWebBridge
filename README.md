Blender Web Bridge



Blender 与 Web 端的实时双向桥接工具

功能特性 • 安装指南 • 使用说明 • 贡献



Blender Web Bridge 是一个强大的开发工具，允许你通过现代 Web 界面与 Blender 进行实时交互。它不仅仅是一个远程代码执行器，更是一个双向的场景同步与编辑器。你可以将 Blender 中的模型和灯光同步到浏览器中预览，在 Web 端调整变换（移动/旋转/缩放），并实时回传给 Blender。

适用于远程渲染控制、参数化生成调试、双屏工作流或构建自定义的 Blender Web UI 控制器。

✨ 功能特性

🔌 核心连接

WebSocket 通信：基于 WebSocket (Port 8081) 的低延迟双向通信。

自动依赖管理：Blender 插件会自动检测并安装所需的 Python 库 (websockets)。

热插拔：无需重启 Blender 即可通过 UI 按钮开启或关闭服务。

🖥️ 场景同步 (Scene Sync)

Mesh 同步：一键提取 Blender 场景中的可见网格数据到 Web 端显示。

灯光同步：支持 Point、Sun、Spot 等灯光类型的同步，并在 Web 端重建光照。

双向变换：在 Web 端使用 Gizmo (坐标轴) 移动、旋转、缩放物体，操作结束后自动更新 Blender 中的对应物体。

大纲视图 (Outliner)：Web 端提供大纲列表，支持点击选中物体。

属性检查器 (Inspector)：实时查看和修改选中对象的坐标数值、灯光颜色及强度。

🐍 远程脚本 (Scripting)

Python 控制台：在 Web 端编写 Python 代码并发送给 Blender 执行。

智能补全 (IntelliSense)：支持 Ctrl + Tab 触发代码补全，自动获取 Blender 上下文建议（如 bpy.data.objects...）。

持久化环境：变量在多次执行间保持存在（例如定义 a=1，下次可直接打印 a）。

🎨 环境与渲染

HDRI 支持：支持在 Web 端加载 .exr 或 .hdr 环境贴图。

环境控制：实时调整 HDRI 的曝光、模糊度、背景强度以及三轴旋转。

灯光控制：支持切换使用“默认场景光”或“Blender 同步光”。

📥 安装指南

1. Blender 插件端

下载 blender_remote_addon.py 文件。

打开 Blender，前往 Edit -> Preferences -> Add-ons。

点击 Install... 选择下载的 .py 文件。

勾选启用 Development: Remote Bridge。

如果是首次运行，插件面板会显示 "Install Dependencies" 按钮，点击它以安装必要的 Python 库。

在 3D 视图顶部的 Header 菜单栏右侧，点击 "Remote: OFF" 按钮使其变为 "Remote: ON"。

2. Web 客户端

克隆或下载本项目。

直接在浏览器中打开 blender_client.html。

注意：虽然可以直接打开文件，但为了更好的性能和模块加载体验，建议使用 Live Server (VS Code 插件) 或 python -m http.server 运行。

🚀 使用说明

建立连接：

确保 Blender 中插件状态为 Remote: ON。

在 Web 页面左上角点击 Connect 按钮，状态灯变绿即为连接成功。

同步场景：

点击 Scene 标签页。

点击 📥 Sync from Blender 按钮。

Blender 中的 Mesh 和 Light 将加载到 Web 场景中。

操作对象：

选中：直接点击 3D 场景中的物体，或在 Outliner 列表中点击。

变换：使用显示的 Gizmo 拖拽物体。松开鼠标后，位置信息会发送回 Blender。

快捷键：

G: 移动模式 (Translate)

R: 旋转模式 (Rotate)

S: 缩放模式 (Scale)

运行脚本：

切换到 Scripting 标签页。

输入 Python 代码（例如 bpy.ops.mesh.primitive_monkey_add()）。

按 Ctrl + Enter 运行代码。

按 Ctrl + Tab 触发自动补全。

🛠️ 技术栈

Blender API (Python): bpy, bmesh, asyncio, websockets

Web Frontend: Vanilla JS, Three.js (r169), Tailwind CSS (CDN)

🤝 贡献

欢迎提交 Issue 或 Pull Request！

Fork 本仓库

创建特性分支 (git checkout -b feature/AmazingFeature)

提交更改 (git commit -m 'Add some AmazingFeature')

推送到分支 (git push origin feature/AmazingFeature)

提交 Pull Request

📄 许可证

本项目采用 MIT 许可证。
